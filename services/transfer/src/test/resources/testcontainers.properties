# Force unix socket strategy; api.version override is applied via -Dapi.version=1.44 argLine in failsafe.
docker.client.strategy=org.testcontainers.dockerclient.UnixSocketClientProviderStrategy
ryuk.disabled=true
